name: Code Analysis

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  analyze:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run PSScriptAnalyzer
        shell: pwsh
        run: |
          Install-Module PSScriptAnalyzer -Force -Scope CurrentUser
          $settingsPath = Join-Path $PWD "PSScriptAnalyzerSettings.psd1"
          $results = Invoke-ScriptAnalyzer -Path ./modules -Recurse -Settings $settingsPath -Severity Error,Warning
          if ($results) {
            $results | Format-Table -AutoSize
            Write-Host "Found $($results.Count) issues"
            exit 1
          } else {
            Write-Host "No issues found!"
          }
